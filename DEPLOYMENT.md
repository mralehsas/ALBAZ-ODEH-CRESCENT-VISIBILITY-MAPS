# دليل النشر على GitHub Pages

## الطريقة الموصى بها: GitHub Actions

1. ارفع جميع محتويات هذا المجلد إلى جذر مستودع GitHub جديد.
2. تأكد أن الفرع الافتراضي اسمه `main`.
3. انتقل إلى `Settings → Pages`.
4. اجعل `Source` مساويًا لـ `GitHub Actions`.
5. ادفع أي تعديل إلى `main` أو شغّل Workflow يدويًا من تبويب `Actions`.
6. راقب Workflow باسم `Deploy Odeh Atlas to GitHub Pages`.

## تحديث البرنامج

استبدل `index.html` ثم ادفع التعديل إلى `main`. ستعمل مهمة التحقق أولًا، وبعدها تُنشر النسخة الجديدة تلقائيًا.

## أخطاء شائعة

- **صفحة 404:** تأكد أن `index.html` موجود في جذر المستودع.
- **فشل النشر:** تأكد أن مصدر Pages هو `GitHub Actions` وأن Actions مفعلة.
- **الموقع يعرض نسخة قديمة:** حدّث الصفحة تحديثًا إجباريًا أو انتظر اكتمال Deployment الجديد.
- **فشل فحص الخط:** لا تضف `data:font` أو ملفات خطوط تجارية إلى النسخة العامة.
